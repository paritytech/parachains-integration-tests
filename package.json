{
  "name": "parachains-integration-test",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Ignacio Palacios Santos",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "yarn build && node dist/cli.js",
    "test": "yarn build && ts-node dist/cli.js -m test",
    "polkadot-launch": "yarn build && node dist/cli.js -m polkadot-launch",
    "polkadot-launch:test": "yarn build && node dist/cli.js -m polkadot-launch-test",
    "dev": "ts-node src/cli.ts",
    "dev:test": "ts-node src/cli.ts -m test",
    "dev:polkadot-launch": "ts-node src/cli.ts -m polkadot-launch",
    "dev:polkadot-launch:test": "ts-node src/cli.ts -m polkadot-launch-test",
    "lint": "prettier -v && prettier --check .",
    "lint:write": "prettier --write ."
  },
  "dependencies": {
    "@polkadot/api": "^7.9.1",
    "@types/node": "^16.7.10",
    "bn.js": "^5.2.0",
    "command-line-args": "^5.2.0",
    "commander": "^9.0.0",
    "dotenv": "^10.0.0",
    "glob": "^7.2.0",
    "lodash": "^4.17.21",
    "polkadot-launch": "^1.9.0",
    "traverse": "^0.6.6",
    "typescript": "^4.4.2",
    "yaml": "^1.10.2"
  },
  "devDependencies": {
    "@types/chai": "^4.2.22",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "chai-bn": "^0.3.0",
    "mocha": "^9.1.3",
    "prettier": "^2.5.1",
    "ts-node": "^10.4.0"
  }
}
